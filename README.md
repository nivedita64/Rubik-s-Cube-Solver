Created **Rubiks-Cube-Solver** with efficient solving in fewer steps
- Project: Rubicks_Cube_Solver  
- Language: C++  
- Purpose: Solve a Rubik’s Cube using smart search methods  
- Includes: different solvers (BFS, DFS, IDA*), pattern databases, tools  
- Example: `main.cpp` shows how to run a solver  
- Build: use CMake to compile the project  
- Run: run the built executable to test the solver
